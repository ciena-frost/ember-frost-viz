<rect class="frost-viz-scale-rect" x={{backgroundArea.x}} y={{backgroundArea.y}} />
{{#each tickElements as |tick|}}
  {{#if hasBlock}}
    {{yield tick}}
  {{else}}
    {{frost-viz/scale/tick tick}}
  {{/if}}
{{/each}}
