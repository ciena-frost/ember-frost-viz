<rect class="frost-viz-scale-rect" x={{rectArea.x}} y={{rectArea.y}} />
{{#each tickElements as |tick|}}
  {{#if hasBlock}}
    {{yield tick}}
  {{else}}
    {{frost-viz/scale/tick tick}}
  {{/if}}
{{/each}}
