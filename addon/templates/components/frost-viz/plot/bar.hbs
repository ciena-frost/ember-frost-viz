<defs>
  <clipPath id="{{clipPathId}}">
    <rect width={{width}} height={{height}} />
  </clipPath>
</defs>

<rect width={{width}} height={{height}} />
<g style={{clipPathStyle}}>
  {{#each bars as |bar|}}
    {{yield bar data}}
  {{/each}}
</g>
